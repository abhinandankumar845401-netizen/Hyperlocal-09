services:
  - type: web
    name: trustlocal-server
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npx tsx src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
